# ViHire Platform CI/CD

This directory represents the `platform-cicd` repository. It owns Jenkins pipeline logic, service metadata, Ansible inventory, deployment playbooks, Docker Compose templates, setup scripts, and platform documentation.

The service catalog is the source of truth for deployable services:

- `vihire-backend`
- `vihire-frontend`

