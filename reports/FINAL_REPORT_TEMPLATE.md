# FINAL REPORT TEMPLATE

## 1. Thông tin chung

- Repo URL: `<GITHUB_REPO_URL>`
- VM1 IP: `<VM1_IP>`
- VM2 IP: `<VM2_IP>`
- Jenkins URL: `http://<VM1_IP>:8080`
- App URL: `http://<VM2_IP>:5000`

## 2. Trạng thái Jenkins

- Jenkins Controller status:
- Jenkins node status:
- Jenkins job name:
- Build number:
- Build result:

## 3. Kết quả demo

- Mở được Jenkins UI:
- Node `docker-agent` online:
- Job chạy đủ 3 stage:
- App mở được endpoint `/`:
- App mở được endpoint `/health`:

## 4. Evidence checklist

- Ảnh Jenkins dashboard
- Ảnh node online
- Ảnh build success
- Ảnh console output stage Deploy
- Ảnh trang `http://<VM2_IP>:5000`
- Ảnh `curl http://<VM2_IP>:5000/health`
