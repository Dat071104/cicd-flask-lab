services:
  web:
    image: cicd-flask-lab:${IMAGE_TAG:-latest}
    container_name: cicd-flask-lab
    restart: unless-stopped
    ports:
      - "5000:5000"
    environment:
      APP_BRANCH: ${APP_BRANCH:-unknown}
      BUILD_NUMBER: ${BUILD_NUMBER:-local}
